{
  "name": "playwright_project_a",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "clean": "node clean.js",
    "clean:win": "if exist playwright-report rd /s /q playwright-report && if exist test-results rd /s /q test-results && if exist .playwright rd /s /q .playwright"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.0.13"
  },
  "dependencies": {
    "dayjs": "^1.11.13"
  }
}
