{
  "name": "playwright_project_a",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "clean": "rm -rf playwright-report test-results .playwright",
    "clean:win": "rd /s /q playwright-report && rd /s /q test-results && rd /s /q .playwright"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.0.13"
  }
}
